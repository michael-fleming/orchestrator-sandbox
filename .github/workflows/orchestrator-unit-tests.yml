# Generated by the orchestrator from .orchestrator/pipeline.yaml (michael-fleming/ci-templates/.github/workflows/test-node.yml@v1).
# Safe to edit: prefer template inputs and .orchestrator/hooks/*.sh. If you edit this file,
# the orchestrator treats it as ejected and stops regenerating it.
# orchestrator-generated: sha256=cc5682f57cdec99c
name: orchestrator-unit-tests
run-name: orchestrator-unit-tests ${{ inputs.task_id }}
on:
  workflow_dispatch:
    inputs:
      task_id:
        required: true
        type: string
      sha:
        required: true
        type: string
permissions:
  contents: read
  packages: write
  id-token: write
jobs:
  run:
    uses: michael-fleming/ci-templates/.github/workflows/test-node.yml@v2
    with:
      task_id: ${{ inputs.task_id }}
      sha: ${{ inputs.sha }}
      node-version: "22"
    secrets: inherit
