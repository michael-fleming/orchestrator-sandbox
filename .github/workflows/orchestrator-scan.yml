# Generated by the orchestrator from .orchestrator/pipeline.yaml (michael-fleming/ci-templates/.github/workflows/scan-grype.yml@v1).
# Safe to edit: prefer template inputs and .orchestrator/hooks/*.sh. If you edit this file,
# the orchestrator treats it as ejected and stops regenerating it.
# orchestrator-generated: sha256=ef7cdc28afc54120
name: orchestrator-scan
run-name: orchestrator-scan ${{ inputs.task_id }}
on:
  workflow_dispatch:
    inputs:
      task_id:
        required: true
        type: string
      sha:
        required: true
        type: string
      target_digest:
        required: true
        type: string
      target_ref:
        required: true
        type: string
permissions:
  contents: read
  packages: write
  id-token: write
jobs:
  run:
    uses: michael-fleming/ci-templates/.github/workflows/scan-grype.yml@v1
    with:
      task_id: ${{ inputs.task_id }}
      sha: ${{ inputs.sha }}
      target_digest: ${{ inputs.target_digest }}
      target_ref: ${{ inputs.target_ref }}
      sbom-format: cyclonedx-json
    secrets: inherit
